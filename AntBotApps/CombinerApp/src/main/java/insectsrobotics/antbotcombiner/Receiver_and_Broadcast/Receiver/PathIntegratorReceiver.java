package insectsrobotics.antbotcombiner.Receiver_and_Broadcast.Receiver;

public class PathIntegratorReceiver {

    /*
    Resources resources;

    public PathIntegratorReceiver(Receiver.ReceiverListener listener, Resources res) {
        super(listener);
        resources = res;
    }

    @Override
    public void onReceive(Context context, Intent intent) {
        if (intent.getAction().equals(resources.getString(R.string.WheelEncoderBroadcast))){
            //TODO: Do something with the information
        }
    }
    */
}

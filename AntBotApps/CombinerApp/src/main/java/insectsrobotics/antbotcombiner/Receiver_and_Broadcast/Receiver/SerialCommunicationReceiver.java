package insectsrobotics.antbotcombiner.Receiver_and_Broadcast.Receiver;

public class SerialCommunicationReceiver {

    /*
    Resources resources;

    public SerialCommunicationReceiver(Receiver.ReceiverListener listener, Resources res) {
        super(listener);
        resources = res;
    }

    @Override
    public void onReceive(Context context, Intent intent) {
        if (intent.getAction().equals(resources.getString(R.string.TaskExecutedBroadcast))){
            //TODO execute Broadcasts to request new Data from SerialApp and VisualApp.
        }
    }
    */
}
